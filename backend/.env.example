# Local development configuration for the Secure Offline Forest Patrol backend.
# This file is NOT tracked by git (.gitignore). Never commit real credentials.
FOREST_ENVIRONMENT=development
FOREST_API_HOST=0.0.0.0
FOREST_API_PORT=8000

# Dedicated forest patrol database (NOT the maritime jalri database).
DATABASE_URL=postgresql+psycopg://kireeti@localhost:5432/forest_patrol
PROD_DB_URL=postgresql://neondb_owner:npg_4S5gzalZJqYB@ep-quiet-waterfall-b5zun413-pooler.c-7.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
# Gateway backhaul shared secret (X-Gateway-Key). Required outside development.
# Local development value only; production must inject its own secret.
FOREST_GATEWAY_INGESTION_KEY=6a57f4931aa1bd8e05205230c897167e85557348760c424416e8d952902a01cd
